-
clap: key(enter)

ex clomp:
    key(!)
    key(space)

spam:
    insert(", ")

whomp:
    insert(". ")

bomp:
    insert("- ")

clomp:
    key(:)
    key(space)

dat bomp:
    insert("-- ")

tab back:
    key(shift-tab)
