-
clap: key(enter)

ex clomp:
    key(!)
    key(space)

spam:
    key(,)
    key(space)

whomp:
    key(.)
    key(space)

bomp:
    key(-)
    key(space)

clomp:
    key(:)
    key(space)

dat bomp:
    insert("--")
    key(space)

tab back:
    key(shift-tab)
